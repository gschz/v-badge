{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "^/$",
      "dest": "/api/index.ts"
    },
    {
      "src": "^/([^/]+)/([^/]+\\.svg)$",
      "dest": "/api/[user]/[repo].ts?user=$1&repo=$2"
    }
  ]
}
